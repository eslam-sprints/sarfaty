# صرفتي — وصف التطبيق

**صرفتي** تطبيق Flutter عربي لإدارة المصروفات الشخصية وتتبع بطاقات الائتمان محلياً على الجهاز، دون حسابات سحابية أو تسجيل دخول. العملة المعروضة حالياً هي الجنيه المصري (`ج.م`). رقم الإصدار في المشروع: `0.2.0+2`.

> ملاحظة من الكود: عند أول تشغيل يبدأ المخزن فارغاً برصيد ابتدائي `0`؛ لا تُزرع بيانات تجريبية تلقائياً في مسار التشغيل العادي رغم ما قد يذكره ملف README.

---

## فكرة التطبيق

صرفتي يساعد المستخدم على الإجابة عن أسئلة يومية بسيطة:

- كم صرفت هذا الشهر؟
- كم بقي من الرصيد المتاح؟
- ما مستحقات بطاقات الائتمان وأقرب موعد سداد؟
- أين يذهب المال (حسب التصنيف وطريقة الدفع)؟

يفصل التطبيق بين **الكاش** و**الكريديت**، ويربط مصروفات الكريديت ببطاقات لها حد ائتماني ويوم استحقاق، مع إمكانية تسجيل سداد كامل أو جزئي من الرصيد المتاح.

---

## تجربة المستخدم

الواجهة بالكامل بالعربية واتجاه **RTL**. اللغة مثبتة على `Locale('ar')` عبر `Directionality`.

التنقل الأساسي عبر شريط سفلي (`NavigationBar`) بأربعة تبويبات داخل `HomeShell` مع `IndexedStack` للحفاظ على حالة كل تبويب. من الشاشة الرئيسية يظهر زر عائم **إضافة مصروف**. شاشات إضافة/تعديل المصروف وقائمة كل المعاملات تُفتح عبر `Navigator.push` وليست تبويبات.

التدفق النموذجي:

1. فتح **الرئيسية** لمراجعة إجمالي الشهر والرصيد والمستحقات وأقرب استحقاق.
2. إضافة مصروف (مبلغ، تصنيف، كاش/كريديت، تاريخ، ملاحظة اختيارية).
3. إدارة البطاقات في **بطاقاتي** ومتابعة الاستخدام والتنبيهات وتسجيل السداد.
4. مراجعة **الملخص** لنمط الصرف الشهري.
5. من **الإعدادات** تغيير المظهر أو تصدير/استيراد نسخة احتياطية JSON.

---

## الشاشات والميزات الحالية

### 1. الرئيسية (`DashboardScreen`)

- إجمالي مصروفات الشهر الحالي.
- الرصيد المتاح.
- إجمالي مستحقات الكريديت.
- أقرب بطاقة عليها مستحق + عدد الأيام حتى موعد السداد.
- آخر 5 مصروفات مع حالة فارغة إن لم يوجد شيء.
- اختصار لإضافة مصروف، ورابط **عرض الكل** لفتح قائمة المعاملات.

### 2. إضافة / تعديل مصروف (`AddExpenseScreen`)

- إدخال المبلغ واختيار التصنيف.
- اختيار طريقة الدفع: كاش أو كريديت (عبر `SegmentedButton`).
- عند الكريديت: اختيار البطاقة (مع التحقق من وجود بطاقة).
- اختيار التاريخ (من 2020 حتى اليوم).
- ملاحظة اختيارية بحد أقصى 80 حرفاً.
- يدعم الإنشاء والتعديل عبر `addExpense` / `updateExpense`.

### 3. كل المعاملات (`TransactionsScreen`)

- قائمة موحّدة للمصروفات وسجلات سداد البطاقات.
- فلاتر: طريقة الدفع، التصنيف، البطاقة (عند الكريديت)، ونطاق تاريخي، مع إمكانية مسح الفلاتر.
- عرض إجمالي النتائج وعددها.
- للمصروفات: تعديل أو حذف مع تأكيد.
- سجلات السداد تظهر في القائمة لكنها غير قابلة للتعديل أو الحذف من الواجهة حالياً.

### 4. بطاقاتي (`CardsScreen`)

- إضافة بطاقة: الاسم، الحد الائتماني، يوم كشف الحساب، يوم الاستحقاق.
- لكل بطاقة: المستخدم / المتبقي / الحد، شريط استخدام، المبلغ المستحق، وتاريخ الاستحقاق المحسوب.
- تنبيهات داخل التطبيق فقط (ليست إشعارات نظام):
  - استخدام ≥ 70% أو ≥ 90% من الحد.
  - استحقاق خلال 7 أيام.
- سداد كامل أو جزئي (`payCard`) بشرط أن يكفي الرصيد المتاح وأن لا يتجاوز المبلغ المستحق.

### 5. الملخص (`SummaryScreen`)

- إجمالي الصرف الشهري.
- أعلى تصنيف لهذا الشهر.
- مجموع سداد البطاقات خلال الشهر.
- نسبة الكاش مقابل الكريديت بشريطي تقدم.
- بطاقة تذكير بأن البيانات محلية على الجهاز.

### 6. الإعدادات (`SettingsScreen`)

- اختيار المظهر: تلقائي (حسب النظام) / فاتح / داكن.
- تصدير البيانات كملف JSON ومشاركته عبر نظام المشاركة.
- استيراد نسخة احتياطية JSON مع حوار تأكيد قبل استبدال كل البيانات الحالية.

### التصنيفات الثابتة

التصنيفات معرفة كـ `enum` وليست قابلة للتخصيص من المستخدم:

| المفتاح | التسمية |
|---------|---------|
| food | أكل ومشروبات |
| transport | مواصلات |
| bills | فواتير |
| shopping | تسوق |
| entertainment | ترفيه |
| health | صحة |
| home | منزل |
| education | تعليم |
| transfers | تحويلات |
| other | أخرى |

لكل تصنيف أيقونة ولون مخصصان في الواجهة.

### منطق مالي أساسي (من `FinanceStore`)

- **الرصيد المتاح** = الرصيد الابتدائي − مجموع مصروفات الكاش − مجموع كل المدفوعات على البطاقات.
- **مستحق البطاقة** = `(openingDue + مصروفات البطاقة − المدفوع)` مع عدم النزول تحت صفر.
- **نسبة الاستخدام** = المستحق ÷ الحد الائتماني.
- نطاق «الشهر» يعتمد على الشهر والتقويم الحالي للمصروفات والسداد والملخص.

### حدود واضحة في النسخة الحالية (حسب الكود)

- لا توجد واجهة لتعديل الرصيد الابتدائي يدوياً (يُحفظ في قاعدة البيانات ويمكن أن يأتي عبر الاستيراد).
- حقل `openingDue` و`statementDay` موجودان في النموذج وقاعدة البيانات؛ إضافة البطاقة من الواجهة تضع `openingDue` عند 0، و`statementDay` يُحفظ دون استخدامه في حساب الاستحقاق (يُعتمد على `dueDay`).
- لا يوجد تعديل/حذف للبطاقات، ولا حذف/تراجع لسجلات السداد.
- لا إشعارات نظام؛ التنبيهات داخل شاشة البطاقات فقط.
- العملة ثابتة على الجنيه المصري في العرض والتصدير.

---

## التصميم

- **Material Design 3** مع `ColorScheme.fromSeed`.
- الهوية اللونية تعتمد على درجات التركواز/التيل:
  - فاتح: بذرة `0xFF0D9488` وسطح `0xFFF7F8F6`.
  - داكن: بذرة `0xFF14B8A6` وسطح `0xFF101817`.
- بطاقات بلا ارتفاع ظل (`elevation: 0`) وزوايا دائرية (~22).
- حقول إدخال ممتلئة بزوايا ~16.
- شريط علوي مسطح بنفس لون السطح.
- في الرئيسية بطاقة بطولية بتدرج تيل.
- مكوّنات مشتركة في `common.dart`: تنسيق المبالغ `money()`، تاريخ مختصر، `MetricCard`، و`ExpenseTile`.
- الأيقونات من Material Icons / Cupertino Icons؛ لا خطوط عربية مخصصة في `pubspec.yaml`.
- أيقونة التطبيق مُعدّة عبر `flutter_launcher_icons` من `assets/app_icon.png`.

---

## التخزين المحلي والخصوصية

### التخزين

- المصدر الأساسي للبيانات: قاعدة **SQLite** عبر `sqflite` في ملف `sarfaty.db` داخل مسار قواعد بيانات الجهاز.
- الجداول: `settings`، `cards`، `expenses`، `payments` مع مفاتيح أجنبية وفهارس على تاريخ المصروف و`card_id`.
- إعدادات محفوظة: `starting_balance` و`theme` (افتراضياً `0` و`system`).
- الطبقة المنطقية: `FinanceStore` (`ChangeNotifier`) يحمّل اللقطة عند الإقلاع ويكتب التغييرات إلى قاعدة البيانات بعد كل عملية مؤثرة.
- الحزمة `shared_preferences` مذكورة في الاعتماديات لكنها غير مستخدمة في كود التطبيق الحالي.

### النسخ الاحتياطي

- التصدير ينتج JSON منسّقاً بإصدار مخطط `schemaVersion: 1` يشمل الرصيد الابتدائي، المظهر، العملة، المصروفات، البطاقات، والمدفوعات.
- الملف يُكتب مؤقتاً ثم يُشارك عبر `share_plus`.
- الاستيراد يتحقق من صحة المخطط ثم يستبدل كل البيانات بعد تأكيد صريح من المستخدم.

### الخصوصية

- لا مصادقة، لا خادم، لا مزامنة سحابية مدمجة.
- البيانات تبقى على الجهاز؛ النقل بين الأجهزة يكون يدوياً عبر ملف النسخة الاحتياطية.
- ملف JSON غير مشفّر؛ من يملكه يمكنه قراءة البيانات المالية، لذلك يجب حفظه في مكان آمن كما تنبّه واجهة الإعدادات.

---

## التقنيات

| الجانب | الاختيار |
|--------|----------|
| الإطار | Flutter (Material 3) |
| اللغة | Dart (`sdk: ^3.8.1`) |
| المنصات في المستودع | Android و iOS |
| إدارة الحالة | `ChangeNotifier` + `ListenableBuilder` عبر مخزن واحد `FinanceStore` |
| قاعدة البيانات | `sqflite` |
| مشاركة الملفات | `share_plus` + `path_provider` |
| اختيار ملف الاستيراد | `file_picker` |
| الاختبارات | `flutter_test` (منها `test/finance_store_test.dart` لمنطق الرصيد والسداد والنسخ الاحتياطي) |
| التحليل | `flutter_lints` |
| أيقونة الإطلاق | `flutter_launcher_icons` |

### هيكل المصدر الرئيسي

```
lib/
  main.dart
  src/
    app.dart
    models/finance_models.dart
    data/local_database.dart
    state/finance_store.dart
    screens/
      home_shell.dart
      dashboard_screen.dart
      add_expense_screen.dart
      transactions_screen.dart
      cards_screen.dart
      summary_screen.dart
      settings_screen.dart
    widgets/common.dart
```

---

## خلاصة

صرفتي تطبيق شخصي محليّ التوجه لإدارة مصروفات الكاش والكريديت وبطاقات الائتمان بواجهة عربية RTL، مع لوحة رئيسية وملخص شهري وفلاتر معاملات ونسخ احتياطي JSON. التخزين دائم عبر SQLite على الجهاز، والخصوصية مبنية على عدم إرسال البيانات لأي خادم، مع مسؤولية المستخدم عن حماية ملف النسخة الاحتياطية عند نقله.
